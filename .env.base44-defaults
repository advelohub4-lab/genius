# Base44 development defaults — placeholders so the app boots without external credentials.
# Real values from /run/base44/app.env (dashboard) override these.
# These are Manus-platform integration vars; replace with real values to enable auth/storage/notifications.

VITE_APP_ID=dev-placeholder-app-id
VITE_OAUTH_PORTAL_URL=https://auth.example.com
OAUTH_SERVER_URL=https://auth.example.com
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
