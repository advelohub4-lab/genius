# Local development defaults for Base44 preview.
# Real secrets are delivered via /run/base44/app.env (overrides these).
# JWT_SECRET is generated by the platform and delivered via app.env.

# Manus platform integrations (optional — leave blank to boot without them).
# Provide real values via the Base44 Secrets dashboard to enable login / forge features.
VITE_APP_ID=
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
