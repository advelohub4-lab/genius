import { useEffect, useRef, useState } from "react";
import { ArrowRight, BookOpen, BriefcaseBusiness, Check, CircleDollarSign, FileCheck2, LockKeyhole, ShieldCheck } from "lucide-react";
import { Link } from "wouter";
import { MarketingHeader } from "@/components/MarketingHeader";

function SectionLabel({ children }: { children: string }) { return <span className="eyebrow">{children}</span>; }
function IconTile({ icon: Icon, tone = "blue" }: { icon: typeof BookOpen; tone?: "blue" | "gold" | "navy" }) { return <span className={`grid h-9 w-9 shrink-0 place-items-center rounded-xl sm:h-10 sm:w-10 ${tone === "gold" ? "bg-[#fff5dd] text-[#a6680b]" : tone === "navy" ? "bg-[#e7edf8] text-[#0e1b33]" : "bg-[#e9f0ff] text-[#1e56d0]"}`}><Icon size={19} strokeWidth={1.8} /></span>; }

const aiCards = [
  { icon: FileCheck2, tone: "blue", eyebrow: "Microtask Training", title: "Train AI Models", description: "Complete short quality-control tasks that help improve model performance and earn pay per task where available.", bullets: ["Data labeling, text annotation, and model evaluation.", "Typical tasks take 1–15 minutes; accuracy affects qualification.", "Availability and eligibility change by project."], cta: "Browse training tasks", href: "/app/learn", image: "/manus-storage/skill2earn-card-ai-training_41471d00.png" },
  { icon: CircleDollarSign, tone: "gold", eyebrow: "Paid Tasks", title: "Earn Money Online", description: "Pick paid microtasks—like prompt testing and text annotation—and get paid per completed task when eligible.", bullets: ["Prompt testing, data labeling, tagging, and transcription.", "Pay varies; review rates and acceptance criteria first.", "Check eligibility and external platform rules."], cta: "View paid tasks", href: "/app/jobs", image: "/manus-storage/skill2earn-card-online-earning_a4a6b7ea.png" },
  { icon: BriefcaseBusiness, tone: "navy", eyebrow: "Work Anywhere", title: "Remote, Flexible Work", description: "Do remote tasks on your schedule—accept work when available and build a reputation for quality.", bullets: ["Model evaluation, prompt testing, and data labeling.", "Requirements differ by project; keep your profile current.", "Follow task-specific instructions to remain eligible."], cta: "Create your profile", href: "/auth/signup", image: "/manus-storage/skill2earn-card-remote-work_6a904d67.png" },
] as const;

type Card = (typeof aiCards)[number];
function SwipeableCardRail({ cards }: { cards: ReadonlyArray<Card> }) {
  const railRef = useRef<HTMLDivElement>(null);
  const [activeIndex, setActiveIndex] = useState(0);
  useEffect(() => {
    const rail = railRef.current;
    if (!rail) return;
    const update = () => { const firstCard = rail.children[0] as HTMLElement | undefined; if (firstCard) setActiveIndex(Math.min(Math.max(Math.round(rail.scrollLeft / Math.max(firstCard.offsetWidth + 16, 1)), 0), cards.length - 1)); };
    rail.addEventListener("scroll", update, { passive: true });
    return () => rail.removeEventListener("scroll", update);
  }, [cards.length]);
  const jumpTo = (index: number) => { const rail = railRef.current; const card = rail?.children[index] as HTMLElement | undefined; if (rail && card) { rail.scrollTo({ left: card.offsetLeft, behavior: "smooth" }); setActiveIndex(index); } };
  return <div className="flex min-h-0 flex-1 flex-col"><div className="flex items-center justify-between gap-3"><div><h2 className="mt-1 font-display text-lg font-semibold tracking-[-.03em] text-white sm:text-2xl">Work anywhere.</h2></div><span className="shrink-0 rounded-full border border-[#dbe4f0] bg-white px-2.5 py-1.5 text-[9px] font-semibold uppercase tracking-[.12em] text-[#64728a]">Swipe to explore</span></div><div ref={railRef} className="mt-2 flex min-h-0 flex-1 snap-x snap-mandatory gap-4 overflow-x-auto pb-2 pr-7 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">{cards.map(({ icon: Icon, tone, eyebrow, title, description, bullets, cta, href, image }, index) => <article key={title} className="safe-card flex min-w-[calc(100vw-48px)] snap-start flex-col overflow-hidden p-3 sm:min-w-[480px] lg:min-w-[540px] sm:p-5"><div className="relative shrink-0 overflow-hidden rounded-xl bg-[#0e1b33]"><img src={image} alt="" className="h-16 w-full object-cover shadow-sm sm:h-36" loading={index === 0 ? "eager" : "lazy"} decoding="async" fetchPriority={index === 0 ? "high" : "auto"} /><span aria-hidden="true" className="pointer-events-none absolute inset-0 bg-gradient-to-t from-[#07101f]/55 via-[#07101f]/10 to-transparent" /><span className="absolute right-2 top-2 rounded-full bg-[#0e1b33]/75 px-2 py-1 text-[9px] font-semibold uppercase tracking-[.1em] text-white shadow-sm backdrop-blur-sm">Task preview</span></div><div className="mt-2 flex items-center gap-2"><IconTile icon={Icon} tone={tone} /><p className="text-[9px] font-bold uppercase tracking-[.14em] text-[#1e56d0]">{eyebrow}</p></div><h3 className="mt-1 font-display text-base font-semibold tracking-[-.03em] text-[#13213a] sm:text-lg">{title}</h3><p className="mt-1 max-h-8 overflow-hidden text-[10px] leading-4 text-[#71809a] sm:mt-2 sm:max-h-10 sm:text-[11px]">{description}</p><ul className="mt-2 hidden max-h-11 gap-1 overflow-hidden border-t border-[#edf1f7] pt-2 sm:grid">{bullets.map((bullet) => <li key={bullet} className="flex gap-1.5 text-[10px] leading-3.5 text-[#53627a]"><Check size={13} className="mt-0.5 shrink-0 text-[#198754]" />{bullet}</li>)}</ul><Link href={href} className="mt-2 inline-flex w-full shrink-0 items-center justify-center gap-2 rounded-xl bg-[#1e56d0] px-3 py-2 text-xs font-bold text-white hover:bg-[#1948b5]">{cta}<ArrowRight size={14} /></Link></article>)}</div><div className="flex h-5 items-center justify-center gap-2"><span className="mr-1 text-[9px] font-bold uppercase tracking-[.12em] text-[#8190a6]">{activeIndex + 1} / {cards.length}</span>{cards.map((card, index) => <button key={card.title} onClick={() => jumpTo(index)} aria-label={`Show card ${index + 1}: ${card.title}`} aria-current={activeIndex === index ? "true" : undefined} className={`h-2 rounded-full transition-all ${activeIndex === index ? "w-7 bg-[#1e56d0]" : "w-2 bg-[#b8c5d8] hover:bg-[#7fa4ee]"}`} />)}</div></div>;
}

export default function Home() {
  return <div className="h-[100svh] overflow-hidden bg-[#0e1b33] text-white"><MarketingHeader /><main className="h-[calc(100svh-72px)] overflow-hidden bg-[#0e1b33]"><section className="hero-glow h-full overflow-hidden bg-[#0e1b33]"><div className="mx-auto flex h-full max-w-7xl flex-col gap-4 px-5 pb-3 pt-2 sm:gap-6 sm:pt-4 lg:grid lg:grid-cols-[minmax(0,.9fr)_minmax(0,1.1fr)] lg:gap-10 lg:px-8 lg:py-4"><div className="shrink-0 lg:flex lg:flex-col lg:justify-center"><h1 className="mt-2 max-w-[700px] font-display text-[2rem] font-semibold leading-[1.02] tracking-[-.045em] text-white sm:mt-4 sm:text-5xl lg:text-[3.35rem] lg:leading-[1.02]">Train AI.<br /><span className="text-[#1e56d0]">Earn money.</span><br />Work online.</h1><p className="mt-3 max-w-[560px] text-[13px] leading-5 text-[#c3d0e2] sm:mt-4 sm:text-[15px] sm:leading-6">Complete paid AI tasks—label data, test prompts, evaluate models—and earn when eligible.</p><div className="mt-3 grid max-w-[520px] grid-cols-2 gap-2 sm:mt-5 sm:gap-3"><div className="rounded-2xl border border-[#cddcf7] bg-white/90 p-3 shadow-sm sm:p-4"><p className="text-[9px] font-bold uppercase tracking-[.14em] text-[#6b82b1]">Estimated target income</p><p className="mt-1 font-display text-xl font-bold text-[#13213a] sm:text-2xl">$5–$10 <span className="text-[10px] font-bold text-[#1e56d0] sm:text-xs">/ day</span></p></div><div className="rounded-2xl border border-[#ecd9a9] bg-[#fffaf0]/90 p-3 shadow-sm sm:p-4"><p className="text-[9px] font-bold uppercase tracking-[.14em] text-[#a6680b]">Suggested work time</p><p className="mt-1 font-display text-xl font-bold text-[#13213a] sm:text-2xl">3–7 <span className="text-[10px] font-bold text-[#a6680b] sm:text-xs">hrs/day</span></p></div></div><p className="mt-2 max-w-[520px] text-[10px] leading-4 text-[#b6c5da] sm:text-[11px]">Target only. Actual earnings vary with task availability, eligibility, skill level, and platform rules.</p><div className="mt-3 flex gap-2 sm:mt-5"><Link href="/auth/signup" className="pressable inline-flex items-center justify-center gap-2 rounded-xl bg-[#1e56d0] px-4 py-3 text-xs font-bold text-white shadow-[0_12px_24px_rgba(30,86,208,.2)] hover:bg-[#1948b5] sm:px-5 sm:text-sm">Start training AI <ArrowRight size={15} /></Link><Link href="/auth/signin" className="pressable inline-flex items-center justify-center rounded-xl border border-[#cbd8ec] bg-white px-4 py-3 text-xs font-bold text-[#1e56d0] hover:bg-[#f6f8fc] sm:px-5 sm:text-sm">Sign in</Link></div><div className="mt-3 hidden items-center gap-5 text-[11px] font-semibold text-[#b6c5da] sm:flex"><span className="inline-flex items-center gap-1.5"><ShieldCheck size={14} className="text-[#198754]" /> Transparent by design</span><span className="inline-flex items-center gap-1.5"><LockKeyhole size={13} className="text-[#1e56d0]" /> Secure account flow</span></div></div><div className="min-w-0 flex min-h-0 flex-1 flex-col rounded-[1.4rem] border border-white/15 bg-white/[.07] p-3 shadow-[0_20px_60px_rgba(0,0,0,.16)] sm:p-4 lg:max-h-[510px] lg:justify-center"><SwipeableCardRail cards={aiCards} /></div></div></section></main></div>;
}
